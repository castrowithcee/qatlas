interface:
  display_name: "Qatlas Core"
  short_description: "Diagnose, Statusline, Ping oder Planungssystem verwalten"
  default_prompt: "Nutze $qatlas-core doctor, um den Qatlas-Projektzustand ohne Änderungen zu prüfen."

policy:
  allow_implicit_invocation: false
